{
  "scripts": {
    "watch": "npx concurrently 'tsc --watch' 'webpack --watch'",
    "start": "npx concurrently 'tsc --watch' 'webpack --watch' 'npx serve'",
    "build": "tsc && webpack --mode=production"
  },
  "dependencies": {
    "html-parsed-element": "^0.4.1",
    "sparse": "github:TheUnlocked/sparse"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^9.0.1",
    "typescript": "^4.3.5",
    "webpack": "^5.46.0",
    "webpack-cli": "^4.7.2"
  }
}
